import 'core-js/stable'
import 'regenerator-runtime/runtime'
import './scss/_main.scss'

const { log, error } = console

async function BBPNotifications({ nonce, authId, dom, baseUrl }) {}

window.addEventListener('load', e => BBPNotifications($BBPN))
